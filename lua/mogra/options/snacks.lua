return {
  -- Big file handling
  bigfile = { enabled = true },

  -- Dashboard on startup
  dashboard = { enabled = true },

  -- File explorer
  explorer = { enabled = true },

  -- Image preview (required for xcodebuild.nvim snapshot previews)
  image = { enabled = true },

  -- Indent guides
  indent = { enabled = true },

  -- Better vim.ui.input
  input = { enabled = true },

  -- Picker (used by xcodebuild.nvim)
  picker = { enabled = true },

  -- Notifications
  notifier = {
    enabled = true,
    timeout = 3000,
  },

  -- Quick file opener
  quickfile = { enabled = true },

  -- Scope detection
  scope = { enabled = true },

  -- Smooth scrolling
  scroll = { enabled = true },

  -- Status column
  statuscolumn = { enabled = true },

  -- Word highlighting and navigation
  words = { enabled = true },

  -- Styles
  styles = {
    notification = {
      wo = { wrap = true },
    },
  },
}
